import Constants from 'expo-constants'

export const PUBLIC_SUPABASE_URL = Constants.expoConfig?.extra?.PUBLIC_SUPABASE_URL ?? ''
export const PUBLIC_REALTIME_URL = Constants.expoConfig?.extra?.PUBLIC_REALTIME_URL ?? ''
export const PUBLIC_SUPABASE_KEY = Constants.expoConfig?.extra?.PUBLIC_SUPABASE_KEY ?? ''
export const PUBLIC_TEST_USER_EMAIL = Constants.expoConfig?.extra?.PUBLIC_TEST_USER_EMAIL ?? ''
